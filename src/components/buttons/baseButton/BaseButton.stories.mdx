import { Meta, Canvas, Story } from '@storybook/addon-docs';

import { ThemedContainer } from '../../helpers';

import BaseButton from './BaseButton';

<Meta title='Components/Buttons/BaseButton' parameters={{ options: { showToolbar: true } }} />

export const Template = (args) => (
	<ThemedContainer theme='light'>
		<BaseButton {...args} />
	</ThemedContainer>
);

# BaseButton

A button component used for user interactions. It provides a versatile interface with customizable features such as size, appearance, and behavior.

#### Design - [Figma](https://www.figma.com/file/e9opoAtQHBo4vFd6u0Co98/Component-Library?type=design&node-id=1-2&mode=design&t=doFoJhh78NiStend-0) | Code - [Github](https://github.com/banyandata-cloud/roots/tree/main/components/buttons/baseButton)

> The BaseButton component renders a button element, encapsulating its behavior and appearance. It is customizable through various props, such as size, disabled, and variant. The onClick prop allows you to define a function to be executed when the button is clicked.

## Usage

Basic import and additional information on how to use this component.

```js
import { BaseButton } from '@banyan_cloud/roots';
```

-   This button component can be used in different parts of the application where user interactions are required, providing consistent and adaptable visual elements for your UI.
-   The BaseButton component also supports the inheritance of props from the BaseCell component.

## Stories

The CSF rendered inside the `Canvas` from which you can see the implementation example.

### Simple BaseButton

The default Base Button.

<Canvas>
	<Story
		name='Default Base Button'
		args={{
			title: 'Button Title',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

## API

**`className`**

Additional CSS class names to be applied to the BaseButton component.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>""</td>
	</tr>
</table>

```jsx
<BaseButton className='additionalClass' />
```

**`component1`**

The first component on the left of the title. Accepts JSX element.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>JSX</td>
		<td>null</td>
	</tr>
</table>

```jsx
<BaseButton component1={<p>Some text</p>} />
```

**`title`**

The text to be displayed within the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>string</td>
		<td>""</td>
	</tr>
</table>

```jsx
<BaseButton title='Submit' />
```

**`component3`**

The third component on the right of the title. Accepts JSX element.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>JSX</td>
		<td>null</td>
	</tr>
</table>

```jsx
<BaseButton component3={<p>Some text</p>} />
```

**`size`**

The size of the button, affecting its dimensions.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'sm', 'md', 'lg', 'auto'</td>
		<td>'sm'</td>
	</tr>
</table>

```jsx
<BaseButton size='sm' />
```

**`flexible`**

Make the button button flexible. It adds the following styles: flex-grow: 1;flex-shrink: 1;min-width: 3rem;

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseButton flexible='flexibleStyleClassname' />
```

**`radius`**

For applying border radius to the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'none', 'default', 'round', 'ellipse'</td>
		<td>'default'</td>
	</tr>
</table>

```jsx
<BaseButton radius='none' />
```

**`disabled`**

If true, the button is disabled and cannot be interacted with.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseButton disabled={false} />
```

**`type`**

The type of the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'button', 'submit', 'reset'</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseButton type='submit' />
```

**`onClick`**

Callback function to be executed when the button is clicked.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Function</td>
		<td>{`() => {}`}</td>
	</tr>
</table>

```jsx
<BaseButton
	onClick={() => {
		console.log('Button clicked');
	}}
/>
```

**`blurOnClick`**

If true, the button will lose focus after being clicked.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>true</td>
	</tr>
</table>

```jsx
<BaseButton blurOnClick={false} />
```

**`variant`**

For applying visual style to the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'contained', 'outlined', 'text'</td>
		<td>'contained'</td>
	</tr>
</table>

```jsx
<BaseButton variant={false} />
```
