import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { InlineLoader } from './InlineLoader';

<Meta title='Components/v2/InlineLoader' parameters={{ options: { showToolbar: true } }} />

# Inline Loader

A versatile inline loading component designed to indicate processing, success, or error states seamlessly inside highly dense layouts like buttons or small forms.

#### Design - [Figma](https://www.figma.com/file/e9opoAtQHBo4vFd6u0Co98/Component-Library) | Code - [Github](https://github.com/banyandata-cloud/roots/tree/main/components/v2/inlineLoader)

> The InlineLoader component provides three fundamental visual states: `loading`, `success`, and `error`. It is completely context-agnostic and inherits text color dynamically.

## Usage

Basic import and additional information on how to use this component perfectly.

```js
import { InlineLoader } from '@banyan_cloud/roots/v2';
```

## States Documentation

The CSF rendered inside the `Canvas` from which you can see the implementation example.

### 1. Loading State
Visually indicates a background task is processing.

<Canvas>
	<Story name='Loading'>
		<div style={{ display: 'flex', flexDirection: 'column', gap: '20px', padding: '20px', background: '#f9f9f9', borderRadius: '4px' }}>
			<InlineLoader status="loading" />
			<InlineLoader status="loading" text="Logging" />
		</div>
	</Story>
</Canvas>

### 2. Success State
Rendered immediately upon the completion of a background task.

<Canvas>
	<Story name='Success'>
		<div style={{ display: 'flex', flexDirection: 'column', gap: '20px', padding: '20px', background: '#f9f9f9', borderRadius: '4px' }}>
			<InlineLoader status="success" />
			<InlineLoader status="success" text="Finished" />
		</div>
	</Story>
</Canvas>

### 3. Error State
Displays prominently when a backend request fails to complete safely.

<Canvas>
	<Story name='Error'>
		<div style={{ display: 'flex', flexDirection: 'column', gap: '20px', padding: '20px', background: '#f9f9f9', borderRadius: '4px' }}>
			<InlineLoader status="error" />
			<InlineLoader status="error" text="Error" />
		</div>
	</Story>
</Canvas>

## API

**`status`** (Required)

Determines the visual icon and animation state.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>VALUES</th>
	</tr>
	<tr>
		<td>String</td>
		<td><code>'loading' | 'success' | 'error'</code></td>
	</tr>
</table>

**`text`** (Optional)

An optional string label appended to the right side of the status icon.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td><code>undefined</code></td>
	</tr>
</table>
