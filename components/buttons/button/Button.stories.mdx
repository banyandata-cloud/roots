import { Meta, Canvas, Story } from '@storybook/addon-docs';

import { ThemedContainer, ToggleIframeContainer } from '../../helpers';
import Button from './Button';
import { CopyIcon, ArrowIcon } from '../../icons';

<Meta title='Components/Buttons/Button' parameters={{ options: { showToolbar: true } }} />

export const Template = (args) => (
	<ThemedContainer theme='light'>
		<Button {...args} />
	</ThemedContainer>
);

# Button

A versatile and customizable button component designed for enabling interactive user experiences with configurable appearance and behavior.

#### Design - [Figma](https://www.figma.com/file/e9opoAtQHBo4vFd6u0Co98/Component-Library?type=design&node-id=1-2&mode=design&t=doFoJhh78NiStend-0) | Code - [Github](https://github.com/banyandata-cloud/roots/tree/main/components/buttons/button)

> The Button component builds upon the BaseButton component, adding more flexibility and visual customization. It allows you to include custom components (leftComponent and rightComponent) beside the button's main content. You can adjust the radius of the button, its size, and even its color using the color prop.

## Usage

Basic import and additional information on how to use this component.

```js
import { Button } from '@banyan_cloud/roots';
```

-   This Button component enhances the capabilities of the BaseButton and can be effectively used across the application's UI to provide interactive elements with varying styles and behaviors.

## Stories

The CSF rendered inside the `Canvas` from which you can see the implementation example.

### Default Button (Outlined)

The default button component.

<Canvas>
	<Story
		name='Button - Outlined'
		args={{
			title: 'Button Name',
			variant: 'outlined',
			size: 'md',
			radius: 'default',
			color: 'primary',
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### Default Button (Contained)

The default button component.

<Canvas>
	<Story
		name='Button - Contained'
		args={{
			title: 'Button Name',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'success',
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### With Left Icon

Button with icon on the left of the title.

<Canvas>
	<Story
		name='With left icon'
		args={{
			title: 'Button Name',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
			leftComponent: () => {
				return <ArrowIcon height='24' width='24' color='#ffffff' />;
			},
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### With Right Icon

Button with icon on the right of the title.

<Canvas>
	<Story
		name='With right icon'
		args={{
			title: 'Button Name',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
			rightComponent: () => {
				return <CopyIcon active height='12' width='12' color='#ffffff' />;
			},
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### With Both side Icon

Button with icons on both sides of the title.

<Canvas>
	<Story
		name='With both side icon'
		args={{
			title: 'Button Name',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
			leftComponent: () => {
				return <ArrowIcon height='24' width='24' color='#ffffff' />;
			},
			rightComponent: () => {
				return <CopyIcon active height='12' width='12' color='#ffffff' />;
			},
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### With Only Icon

Button with only icon and no title.

<Canvas>
	<Story
		name='With only icon'
		args={{
			title: '',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
			leftComponent: () => {
				return <ArrowIcon height='24' width='24' color='#ffffff' />;
			},
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### Disabled

Disabled button with no interaction.

<Canvas>
	<Story
		name='Disabled'
		args={{
			title: 'Button Name',
			variant: 'contained',
			size: 'md',
			radius: 'default',
			color: 'primary',
			disabled: true,
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

## Try in the Code Editor

This embeded `CodeSandBox` is external third party code editor `iframe` shared publicly. It is advised to create an account with banyancloud mail id to create/fork this sandbox.

<ToggleIframeContainer>
	<iframe
		src='https://codesandbox.io/embed/banyancloud-Button-2m9sk7?fontsize=14&hidenavigation=1&theme=dark'
		style={{
			width: '100%',
			height: '500px',
			border: 0,
			borderRadius: '4px',
			overflow: 'hidden',
		}}
		title='BanyanCloud - Button'
		allow='accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking'
		sandbox='allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts'></iframe>
</ToggleIframeContainer>

## API

**`className`**

Additional CSS class names to be applied to the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>""</td>
	</tr>
</table>

```jsx
<Button className='additionalClass' />
```

**`component1`**

The first component on the left of the title. Accepts JSX element.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>JSX</td>
		<td>null</td>
	</tr>
</table>

```jsx
<Button component1={<p>Some text</p>} />
```

**`title`**

The text to be displayed within the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>string</td>
		<td>""</td>
	</tr>
</table>

```jsx
<Button title='Submit' />
```

**`component3`**

The third component on the right of the title. Accepts JSX element.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>JSX</td>
		<td>null</td>
	</tr>
</table>

```jsx
<Button component3={<p>Some text</p>} />
```

**`size`**

The size of the button, affecting its dimensions.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'sm', 'md', 'lg', 'auto'</td>
		<td>'sm'</td>
	</tr>
</table>

```jsx
<Button size='sm' />
```

**`flexible`**

Make the button button flexible. It adds the following styles: {flex-grow: 1;flex-shrink: 1;min-width: 3rem;}

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>false</td>
	</tr>
</table>

```jsx
<Button flexible={false} />
```

**`radius`**

For applying border radius to the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'none', 'default', 'round', 'ellipse'</td>
		<td>'default'</td>
	</tr>
</table>

```jsx
<Button radius='none' />
```

**`disabled`**

If true, the button is disabled and cannot be interacted with.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>false</td>
	</tr>
</table>

```jsx
<Button disabled={false} />
```

**`type`**

The type of the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'button', 'submit', 'reset'</td>
		<td>false</td>
	</tr>
</table>

```jsx
<Button type='submit' />
```

**`onClick`**

Callback function to be executed when the button is clicked.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Function</td>
		<td>{`() => {}`}</td>
	</tr>
</table>

```jsx
<Button
	onClick={() => {
		console.log('Button clicked');
	}}
/>
```

**`blurOnClick`**

If true, the button will lose focus after being clicked.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>true</td>
	</tr>
</table>

```jsx
<Button blurOnClick={false} />
```

**`variant`**

For applying visual style to the button.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>'contained', 'outlined', 'text'</td>
		<td>'contained'</td>
	</tr>
</table>

```jsx
<Button variant='outlined' />
```
