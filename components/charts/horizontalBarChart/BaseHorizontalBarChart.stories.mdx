import { Meta, Canvas, Story } from '@storybook/addon-docs';

import { COLORS } from '../../../styles';
import { ThemedContainer, ToggleIframeContainer } from '../../helpers';
import BaseHorizontalBarChart from './BaseHorizontalBarChart';

import {
	sampleData,
	sampleData2,
	onBarClick,
	sampleV2Data,
	formatData,
} from './BaseHorizontalBarChartData.js';

<Meta title='Components/HorizontalBarChart' parameters={{ options: { showToolbar: false } }} />

export const Template = (args) => (
	<ThemedContainer
		{...args}
		style={{
			height: '400px',
		}}>
		<BaseHorizontalBarChart
			{...args}
			onEvents={{
				click: onBarClick,
			}}
			tooltip={{
				trigger: 'item',
				formatter: (param) => {
					// eslint-disable-next-line max-len
					return `${param.marker} ${param.name} ${
						sampleData.metaData.controlsApplied[param.name].x1
					}`;
				},
			}}
		/>
	</ThemedContainer>
);

# Horizontal Bar Chart

A visual Representation of Data that compares it with others in similar Category through Bars, where Bar Height represents the count of represented value.

#### Design - [Figma](https://www.figma.com/file/Cx1dHCv47N2i35IP8wNz1u/Charts?type=design&node-id=13-35013&mode=design&t=CHtnZmqsfkOQE9cx-0) | Code - [Github](https://github.com/banyandata-cloud/roots/tree/main/components/charts/horizontalBarChart)

> This components uses the `echarts` utility library to use the Basic Visual Components and properties to customise the same for required use case and `EChartsReactCore` utility library to Wrap the Charts for React Projects.

## Usage

Basic import and additional information on how to use this component.

```js
import { BaseHorizontalBarChart } from '@banyan_cloud/roots';
```

-   The Horizontal Bar Chart is just like any other chart with standard `chartData` and `metaData` values alongwith props to control customization.
-   The `className` of the Chart can be adjusted in the implementaion code to match its flexibility with respect to its container.

## Stories

The CSF rendered inside the `Canvas` from which you can see the implementation example.

### Default Horizontal Bar Chart

<Canvas>
	<Story
		name='Default'
		args={{
			title: 'Title',
			gridContainLabel: false,
			gridOptions: {
				left: 10,
				right: 10,
				bottom: 50,
				top: 50,
			},
			xAxisShow: false,
			seriesData: sampleData,
			yAxisLabelShow: false,
			ySplitLineShow: false,
			yAxisLineShow: false,
			yAxisTickShow: false,
			barWidth: '50%',
			cursor: 'default',
			stacked: true,
			seriesOption: [
				{
					stackIndex: 1,
					color: COLORS.success,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: true,
						formatter(param) {
							return param.name;
						},
					},
				},
				{
					stackIndex: 2,
					color: COLORS.error,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: false,
					},
				},
				{
					stackIndex: 3,
					color: COLORS.warning,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: false,
					},
				},
			],
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### V2 Horizontal Bar Chart

<Canvas>
	<Story
		name='V2'
		args={{
			title: 'Title',
			gridContainLabel: false,
			gridOptions: {
				left: 10,
				right: 10,
				bottom: 50,
				top: 50,
			},
			xAxisShow: false,
			seriesData: formatData(sampleV2Data),
			yAxisLabelShow: false,
			ySplitLineShow: false,
			yAxisLineShow: false,
			yAxisTickShow: false,
			barWidth: '50%',
			cursor: 'default',
			stacked: true,
			sort: 'x1',
			seriesOption: [
				{
					stackIndex: 1,
					color: '#B6660A',
					label: {
						show: true,
						position: 'insideLeft',
						color: 'white',
						formatter(param) {
							return param.name;
						},
					},
					emphasis: {
						disabled: true,
					},
					itemStyle: {
						borderRadius: [5, 0, 0, 5],
					},
				},
				{
					stackIndex: 2,
					color: '#F9E5CF',
					label: {
						show: true,
						color: 'black',
						position: 'insideRight',
						formatter: (param) => {
							return sampleV2Data?.chartData?.[param.name]?.x1;
						},
					},
					emphasis: {
						disabled: true,
					},
					itemStyle: {
						borderRadius: [0, 5, 5, 0],
					},
				},
			],
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### Horizontal Bar Chart ( Gradient )

Horizonatal Bar Chart with Gradient in Bars

<Canvas>
	<Story
		name='Bar Gradient'
		args={{
			title: 'Title',
			gridContainLabel: false,
			gridOptions: {
				left: 10,
				right: 10,
				bottom: 50,
				top: 50,
			},
			xAxisShow: false,
			seriesData: sampleData2,
			yAxisLabelShow: false,
			ySplitLineShow: false,
			yAxisLineShow: false,
			yAxisTickShow: false,
			barWidth: '50%',
			cursor: 'default',
			stacked: true,
			seriesOption: [
				{
					stackIndex: 1,
					showBackground: false,
					maxvalueStyle: {
						value: 100,
						itemStyle: {
							color: 'white',
							// opacity: 0,
						},
					},
					color: [
						1,
						0,
						0,
						0,
						[
							{
								offset: 0,
								color: 'rgb(124, 255, 185)',
							},
							{
								offset: 1,
								color: 'rgb(26, 165, 91)',
							},
						],
					],
					itemStyle: {
						borderRadius: 100,
					},
					label: {
						show: false,
					},
				},
			],
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

### Loading State in Horizontal Bar Chart

<Canvas>
	<Story
		name='Loading State'
		args={{
			title: 'Title',
			gridContainLabel: false,
			gridOptions: {
				left: 10,
				right: 10,
				bottom: 50,
				top: 50,
			},
			xAxisShow: false,
			seriesData: sampleData,
			yAxisLabelShow: false,
			ySplitLineShow: false,
			yAxisLineShow: false,
			yAxisTickShow: false,
			barWidth: '50%',
			cursor: 'default',
			stacked: true,
			loading: true,
			seriesOption: [
				{
					stackIndex: 1,
					color: COLORS.success,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: true,
						formatter(param) {
							return param.name;
						},
					},
				},
				{
					stackIndex: 2,
					color: COLORS.error,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: false,
					},
				},
				{
					stackIndex: 3,
					color: COLORS.warning,
					backgroundStyle: {
						color: 'transparent',
					},
					label: {
						show: false,
					},
				},
			],
		}}>
		{Template.bind({})}
	</Story>
</Canvas>

## Try in the Code Editor

This embeded `CodeSandBox` is external third party code editor `iframe` shared publicly. It is advised to create an account with banyancloud mail id to create/fork this sandbox.

<ToggleIframeContainer>
	<iframe
		src='https://codesandbox.io/s/falling-hill-wmr9lx?file=/src/App.js'
		style={{ width: '100%', height: 500 }}
		title='xenodochial-moon-5y668f'
		allow='accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking'
		sandbox='allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts'></iframe>
</ToggleIframeContainer>

## API

**`loading`**

loading indicates the waiting state of chart while data is loading

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart loading={false} />
```

**`fallback`**

The fallback indicates whether to show Skeleton component or not in case of Exception

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart fallback={false} />
```

**`title`**

An optional Key to give a title to the Chart.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>''</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart title='Title' />
```

**`titleOptions`**

Object that specifies customization of Chart Title.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>object</td>
		<td>{}</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	titleOptions={{
		textStyle: {
			color: '#A6A6A6',
			fontWeight: 600,
			fontSize: 14,
		},
	}}
/>
```

**`gridOptions`**

Object that set Chart Layout Dimensions from Left, Right, Top and Bottom.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>object</td>
		<td>
			{JSON.stringify({
				left: 0,
				right: 0,
				bottom: 0,
				top: 0,
			})}
		</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	gridOptions={{
		left: 50,
		right: 50,
		bottom: 50,
		top: 100,
	}}
/>
```

**`gridContainLabel`**

An optional key to resize the Chart Layout in order to include 'Title' in display.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Boolean</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart gridContainLabel={false} />
```

**`tooltip`**

Tooltip key can be used to format and display Data in Tooltip for Charts, it can be triggered through several events as input by dev.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>object</td>
		<td>{}</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	tooltip={{
		show: true,
		trigger: 'item',
		axisPointer: {
			type: 'cross',
			label: {
				backgroundColor: '#6a7985',
			},
		},
		triggerOn: 'mousemove',
	}}
/>
```

**`seriesData`**

The Actual Data which will be displayed in the Chart. It must have 2 keys - 'chartData' and 'metaData'.
The chartData can have any number of keys representing Data in object.
The metaData can have any other required Data.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>REQUIRED</th>
	</tr>
	<tr>
		<td>Object</td>
		<td>{}</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	seriesData={{
		chartData: {
			'Audit, Logging and Monitering': {
				x1: 40,
			},
		},
	}}
/>
```

**`yAxisLabelShow`**

Key to decide whether to show or hide Y-Axis Label

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart yAxisLabelShow={false} />
```

**`xAxisShow`**

Key to decide whether to show or hide X-Axis Division Line

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart xAxisLineShow={false} />
```

**`ySplitLineShow`**

Key to decide whether to show or hide Y-Axis Split Line

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart ySplitLineShow={false} />
```

**`yAxisLineShow`**

Key to decide whether to show or hide Y-Axis Line

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart yAxisLineShow={false} />
```

**`yAxisTickShow`**

Key to decide whether to show or hide Y-Axis Tick

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart yAxisTickShow={false} />
```

**`onEvents`**

Callback to control 'Click' event on HeatMap

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Function</td>
		<td>() => {}</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	onEvents={{
		click: functionName,
	}}
/>
```

**`stacked`**

Key to decide whether the Bars are stacked or not.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseAreaChart stacked={false} />
```

**`cursor`**

Key to modify 'cursor' UI, it supports all standard css cursor options.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>default</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart cursor='default' />
```

**`seriesOption`**

This key contains an Array of Objects where each Object maps to each Data-Value and allows customization of them individually.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>REQUIRED</th>
	</tr>
	<tr>
		<td>Array</td>
		<td>[]</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart seriesOption=[
		{
			stackIndex: 1,
			color: COLORS.success,
			backgroundStyle: {
				color: 'transparent',
			},
			label: {
				show: true,
				formatter(param) {
					return param.name;
				},
			},
		},
	] />
```

**`inverse`**

This option decides whether to show the Data/Bars in reverse Order.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>REQUIRED</th>
	</tr>
	<tr>
		<td>Bool</td>
		<td>false</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart inverse={false} />
```

**`legend`**

Legend key is used to customise Chart Legend and the it's data.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Object</td>
		<td>
			{JSON.stringify({
				show: false,
			})}
		</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart
	legend={{
		show: false,
	}}
/>
```

**`seriesName`**

Optional key to specify any name to the HeatMap series.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>''</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart seriesName='Permission Count' />
```

**`minBarHeight`**

Optional key to give Minimum height to the Bars in pixels if no data is available.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>Number</td>
		<td>0</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart minBarHeight={2} />
```

**`style`**

Key to change styles thorugh standard CSS values.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>object</td>
		<td>
			{JSON.stringify({
				width: '100%',
				height: '100%',
			})}
		</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart style={{ width: '90%' }} />
```

**`className`**

Key to change style through external className.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>''</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart className='chart-area' />
```

**`theme`**

The color theme of the container to select between `light` or `dark` mode.

<table style={{ width: '100%' }}>
	<tr>
		<th style={{ textAlign: 'left', width: '50%' }}>TYPE</th>
		<th style={{ textAlign: 'left', width: '50%' }}>DEFAULT</th>
	</tr>
	<tr>
		<td>String</td>
		<td>"dark"</td>
	</tr>
</table>

```jsx
<BaseHorizontalBarChart theme='light' />
```
